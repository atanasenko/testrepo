flows:
  default:
  - codegate()

  something(ui test):
  - try:
    - echo hello
    - try:
      - exit 1
      catch:
      - echo catch
      - fail: "$flowError foo"
      finally:
      - echo "1 ${flowError}"
    finally:
    - echo "2 ${flowError}"
