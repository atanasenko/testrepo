flows:
  default:
  - codegate()

  something(ui test):
  - try:
    - echo hello
    - try:
      - exit 1
      catch:
      - echo catch
      - fail: "$flowErrorMessage foo"
      finally:
      - echo "1 ${flowErrorMessage}"
    finally:
    - echo "2 ${flowErrorMessage}"
