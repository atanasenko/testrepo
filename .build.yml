envs:
  global:
    variables:
      bar:
        one: 1
        two: 2
        three: [foo, bar]

flows:
  default:
  - foreach(one, two, three): |
      echo "%key%=%value%"
      sleep 1

  - foreach($bar): |
      echo "%key%=%value%"
      sleep 1

  - foreach('%{bar.three}'): |
      echo "%key%=%value%"
      sleep 1
